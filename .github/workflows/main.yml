name: LinkGeç Test & Deploy

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Test app startup
      run: |
        timeout 10 python app.py &
        sleep 5
        curl -f http://localhost:5001 || exit 1
        echo "✅ App başarıyla çalıştı!"
        
    - name: Check syntax
      run: |
        python -m py_compile app.py
        echo "✅ Syntax kontrolü başarılı!"

  deploy-info:
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Deployment Ready
      run: |
        echo "🚀 Deployment için hazır!"
        echo "📋 Manuel deployment adımları:"
        echo "1. VPS'e git clone yap"
        echo "2. pip install -r requirements.txt"
        echo "3. python app.py"
        echo "4. Port 5001'i aç"
